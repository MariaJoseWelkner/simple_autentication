<!DOCTYPE html>
<html>
  <head>
    <title>SimpleAutentication</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <%
=begin%>
 current_user porque el modelo en este caso se llama user, si el modelo fuera client por ejemplo seria current_client 
<%
=end%>
    <% if current_user %> 
      <%= link_to "Log Out", destroy_user_session_path , data: { turbo_method: :delete, turbo_confirm: "Are you sure?"} %>
    <% else %>
      <%= link_to 'Log In', new_user_session_path %>
    <% end %>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    <%= yield %>
  </body>
</html>
